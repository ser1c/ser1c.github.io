---
// For a production site, you would import these from a content collection
// For now, we'll define them directly
const papers = [
  {
    id: 1,
    title: "Effect of Remittances on Farm Productivity after a natural disaster",
    authors: "with Prakash Pathak and Agnese Romiti",
    status: "Working Paper",
    slug: "remittances-farm-productivity",
    links: {
      details: "#",
      paper: "#",
      slides: "#",
    }
  },
  {
    id: 2,
    title: "Effects of public health insurance : Evidence from Nepal",
    authors: "",
    status: "Working Paper",
    slug: "public-health-insurance-nepal",
    links: {
      details: "#",
      paper: "#",
      slides: "#",
    }
  }
];
---

<section id="papers" class="mt-12 scroll-mt-24">
  <h2 class="text-2xl font-bold pb-2 border-b border-dashed border-gray-300 mb-6">Working Papers</h2>
  
  <div class="space-y-8">
    {papers.map(paper => (
      <div>
        <p>{paper.id}. <strong>{paper.title}</strong></p>
        {paper.authors && <p>{paper.authors}</p>}
        <p class="italic">{paper.status}</p>
        <div class="mt-2 space-x-2">
          <a href={`/papers/${paper.slug}`} class="text-gray-700 hover:text-blue-800">Details</a> | 
          <a href={paper.links.paper} class="text-gray-700 hover:text-blue-800">Paper</a> | 
          <a href={paper.links.slides} class="text-gray-700 hover:text-blue-800">Slides</a>
        </div>
      </div>
    ))}
  </div>
</section>
