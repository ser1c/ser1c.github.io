---
const { name, url, includeRightBar = true } = Astro.props
---

<Fragment>
	<a href={url} class="border-b border-dhaka-yellow hover:border-dhaka-red transition-colors no-underline">{name}</a>
	{
		includeRightBar && <span class="text-size--1 px-[4px]">|</span>
	}
</Fragment>